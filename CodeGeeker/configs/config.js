import { domain } from './website';
import { categories, authors, pictures } from './data';

export const config = {
    domain: domain,
    categories: categories,
    authors: authors,
    pictures: pictures,
}